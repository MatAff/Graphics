#pragma once

#include <stdio.h>
#include <iostream>
#include <string>
#include <vector>

class QuadEq
{
public:

    static std::vector<float> solve(float a, float b, float c);
    //static float minPositive(int res[]);

};
